development:
  client_id: "tasks_service"
  brokers:
    - <%= ENV['KAFKA_BROKER'] %>

test:
  client_id: "tasks_service"
  brokers:
    - <%= ENV['KAFKA_BROKER'] %>

production:
  client_id: "tasks_service"
  brokers:
    - <%= ENV['KAFKA_BROKER'] %>
